import Wildberries from './index.js';
import dotenv from 'dotenv';
dotenv.config();
const WB = new Wildberries(process.env.TOKEN, {
    statisticsToken: process.env.STATISTICS_TOKEN,
});
// WB.getFbsOrders('2021-03-29T13:01:04+03:00')
//     .then(data => {
//         console.log(data.length);
//     })
//     .catch(error => {
//         console.log(error);
//     });
WB.getCardsAll().then(data => {
    const flatCards = WB.makeCardsFlat(data);
    // console.log(flatCards.filter(x => x.id === '8697e20c-2a79-5d92-80cc-a9448ac102d4').length);
    flatCards.forEach(card => {
        const barcode = card._nomenclature._variation.barcodes && card._nomenclature._variation.barcodes[0];
        if (!barcode) {
            console.log(JSON.stringify(card, null, 2));
            // throw '!barcode';
        }
        // console.log(barcode);
    });
});
//# sourceMappingURL=demo.js.map